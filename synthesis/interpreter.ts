import { ValueType, RuntimeVal } from "./values"
import { NodeType, Statement } from "../analysis/ast"

export function evaluate(astNode: Statement): RuntimeVal {
    switch (astNode.kind) {
        
    }
}